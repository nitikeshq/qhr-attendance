{
  "name": "qhr-attendance-backend",
  "version": "0.1.0",
  "private": true,
  "description": "Local Express scaffold for the QHR attendance API.",
  "main": "src/server.js",
  "scripts": {
    "dev": "node --watch src/server.js",
    "start": "node src/server.js",
    "seed": "node scripts/seed.js",
    "lint": "node scripts/check-syntax.js",
    "test": "node --test --test-concurrency=1"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.7",
    "express": "^4.21.2",
    "helmet": "^8.0.0",
    "morgan": "^1.10.0"
  },
  "engines": {
    "node": ">=20"
  }
}
